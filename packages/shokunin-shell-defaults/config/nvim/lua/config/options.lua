-- LazyVim auto-sources this for user option overrides.
-- See https://www.lazyvim.org/configuration/general.

-- Use fzf-lua instead of telescope by default — pairs with the
-- lang.editor.fzf extra above and keeps muscle memory with shell `fzf`.
vim.g.lazyvim_picker = "fzf"
