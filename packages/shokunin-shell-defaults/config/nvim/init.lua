-- Shokunin neovim defaults.
--
-- Built on the LazyVim starter (https://github.com/LazyVim/starter,
-- Apache-2.0). Your overrides live in lua/plugins/ and lua/config/ —
-- both survive package upgrades because this file is only dropped into
-- /etc/skel/.config/nvim/ at account creation, never overwritten later.
require("config.lazy")
