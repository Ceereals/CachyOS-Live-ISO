// Placeholder Quickshell entry point.
//
// This file is loaded by the `quickshell` binary at session start. The
// real config will define a top bar, launcher, notifications, etc. For
// now it's a minimal QML document so the package has something to ship.
//
// Reference: https://quickshell.outfoxxed.me/docs/

import Quickshell

ShellRoot {
    // Empty shell — replace with PanelWindow / Variants / etc. when we
    // start designing the actual UI. See OPEN_QUESTIONS.md §2 on how we
    // intend to ship config updates without clobbering user edits.
}
